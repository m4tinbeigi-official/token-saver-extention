# Auto-Push Rule

After completing any user task that results in file modifications, additions, or deletions, you MUST automatically run the `autopush.sh` script to commit and push the changes to GitHub without waiting for the user to explicitly ask.

## How to use:
- Run `./autopush.sh "[Short description of changes]"` using your `run_command` tool.
